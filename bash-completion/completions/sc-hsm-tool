# this was auto-generated by OpenSC/doc/tools/Makefile.am
_sc_hsm_tool()
{
    COMPREPLY=()
    local cur prev split=false
    _get_comp_words_by_ref -n : cur prev

    if type _init_completion &>/dev/null; then
        _init_completion -s && split=true
    else
        _split_longopt && split=true
    fi

    opts="		--bio-server1 --bio-server2 --create-dkek-share --dkek-shares --export-for-pub-key-auth --force --import-dkek-share --initialize --key-reference --label --password --pin --pin-retry --public-key-auth --public-key-auth-status --pwd-shares-threshold --pwd-shares-total --reader --register-public-key --required-pub-keys --so-pin --unwrap-key --verbose --wait --wrap-key -C -I -K -S -U -W -X -e -g -l -n -r -s -v -w "

    if [ ${COMP_CWORD} -eq 1 ]; then
        COMPREPLY=( $(compgen -W "${opts}" -- ${cur}) )
        return 0
    fi

    case "${prev}" in
        		!*)
            _filedir so
            return 0
            ;;
        		--create-dkek-share|--import-dkek-share|--unwrap-key|--wrap-key|-C|-I|-U|-W)
            _filedir
            return 0
            ;;
        		--pin|--so-pin|--password)
            COMPREPLY=( $( compgen -W "$(printenv | cut -d = -f 1 | xargs printf 'env:%s ')" -- $cur ) )
            __ltrim_colon_completions "$cur"
            return 0
            ;;
        		--bio-server1|--bio-server2|--create-dkek-share|--dkek-shares|--export-for-pub-key-auth|--import-dkek-share|--label|--password|--pin|--pin-retry|--public-key-auth|--pwd-shares-threshold|--pwd-shares-total|--reader|--register-public-key|--required-pub-keys|--so-pin|--unwrap-key|--wrap-key|-C|-I|-K|-U|-W|-e|-g|-l|-n|-r|-s)
            return 0
            ;;
        *)
            COMPREPLY=($(compgen -W "${opts}" -- ${cur}))
            return 0
            ;;
    esac

    $split && return 0

    if [[ "$cur" == -* ]]; then
        _longopt $1
        return
    fi

    _filedir
}
complete -F _sc_hsm_tool sc-hsm-tool


# Local variables:
# mode: shell-script
# sh-basic-offset: 4
# sh-indent-comment: t
# indent-tabs-mode: nil
# End:
# ex: ts=4 sw=4 et filetype=sh
