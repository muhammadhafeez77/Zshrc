# this was auto-generated by OpenSC/doc/tools/Makefile.am
_cryptoflex_tool()
{
    COMPREPLY=()
    local cur prev split=false
    _get_comp_words_by_ref -n : cur prev

    if type _init_completion &>/dev/null; then
        _init_completion -s && split=true
    else
        _split_longopt && split=true
    fi

    opts="		--app-df --create-key-files --create-pin-files --exponent --generate-key --key-num --list-keys --modulus-length --prkey-file --pubkey-file --read-key --reader --verbose --verify-pin --wait -P -R -V -a -c -e -g -k -l -m -p -r -u -v -w "

    if [ ${COMP_CWORD} -eq 1 ]; then
        COMPREPLY=( $(compgen -W "${opts}" -- ${cur}) )
        return 0
    fi

    case "${prev}" in
        		!*)
            _filedir so
            return 0
            ;;
        		!*)
            _filedir
            return 0
            ;;
        		!*|--password)
            COMPREPLY=( $( compgen -W "$(printenv | cut -d = -f 1 | xargs printf 'env:%s ')" -- $cur ) )
            __ltrim_colon_completions "$cur"
            return 0
            ;;
        		--app-df|--create-key-files|--create-pin-files|--exponent|--key-num|--modulus-length|--prkey-file|--pubkey-file|--reader|-P|-a|-c|-e|-k|-m|-p|-r|-u)
            return 0
            ;;
        *)
            COMPREPLY=($(compgen -W "${opts}" -- ${cur}))
            return 0
            ;;
    esac

    $split && return 0

    if [[ "$cur" == -* ]]; then
        _longopt $1
        return
    fi

    _filedir
}
complete -F _cryptoflex_tool cryptoflex-tool


# Local variables:
# mode: shell-script
# sh-basic-offset: 4
# sh-indent-comment: t
# indent-tabs-mode: nil
# End:
# ex: ts=4 sw=4 et filetype=sh
